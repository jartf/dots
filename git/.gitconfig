[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[user]
	name = Jarema
	signingKey = ~/.ssh/git.pub
[credential]
	helper = store
  username = jartf
[gpg]
	format = ssh
[commit]
	gpgSign = true
